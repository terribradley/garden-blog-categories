{{#if addNewCategory}}
  <h1>New Category</h1>
    <div>
      <form>
        <div class="form-group">
          <label for="name">Category Name</label>
          {{input value=name id="name"}}
        </div>
        <button {{action 'save1'}}>Save</button>
      </form>
    </div>
{{else}}
    <button {{action 'newCategoryForm'}}>New Category</button>
{{/if}}
